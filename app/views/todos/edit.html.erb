<% provide(:title, "Edit") %>

<h1>Edit</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">

    <%= form_with(url: todo_url, method: :put, local: true) do |f| %>

      <%= f.label :title %>
      <%= f.text_field :title, class: "form-control" %>

      <%= f.submit "Update", class: "btn btn-primary" %>
    
    <% end %>
  
  </div>
</div>
